export enum UnitsEnum {
  Celsius = "Celsius",
  Fahrenheit = "Fahrenheit",
}

export enum ThemeEnum {
  Light = "light",
  Dark = "dark",
}

export enum ThunkStatusEnum {
  IDLE = "idle",
  SUCCESS = "success",
  FAILED = "failed",
  LOADING = "loading",
}
